# VidX Marketplace - Environment Variables Template
# Copy this to .env and fill in your values

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-generate-with-python-secrets

# Server Configuration
HOST=127.0.0.1
PORT=5000

# CORS Settings (for local development)
CORS_ORIGIN=http://127.0.0.1:5000

# Database (when you add PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/vidx

# Azure Storage (when you add blob storage)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_STORAGE_CONTAINER=vidx-videos

# Production overrides (set in Azure App Service)
# FLASK_ENV=production
# CORS_ORIGIN=https://vidx-marketplace.azurewebsites.net
